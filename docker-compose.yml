version: '3'
services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - "${PERSIST_DIR}/data:/app/data"
      - "${PERSIST_DIR}/static/images:/app/static/images"
    restart: unless-stopped
